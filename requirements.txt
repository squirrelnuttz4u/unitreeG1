# Unitree G1 Windows Control Application Dependencies

# Unitree SDK (G1-specific) - OPTIONAL
# WARNING: This package does NOT work on native Windows!
#
# Windows users have 3 options:
# 1. Use WSL2 (recommended) - Install SDK inside WSL2 Ubuntu
# 2. Run in simulation mode - Skip SDK installation entirely
# 3. Build from source (advanced) - Requires CMake, C++ tools
#
# Linux/WSL2 users can install from source:
#   git clone https://github.com/unitreerobotics/unitree_sdk2_python.git
#   cd unitree_sdk2_python
#   pip install -e .
#
# If SDK is not installed, the application will run in simulation mode.
# unitree-sdk2py>=1.0.0  # Commented out - install manually or use simulation mode

# GUI Framework
customtkinter>=5.2.0
Pillow>=10.0.0

# Video Processing
opencv-python>=4.8.0
numpy>=1.24.0

# Audio Processing - For voice control (optional)
# NOTE: On Ubuntu, install system package first: sudo apt install portaudio19-dev
# On Windows, pyaudio may fail - use: pip install pipwin && pipwin install pyaudio
# If audio fails to install, the app will still work without voice control
pyaudio>=0.2.13  # May fail on some systems - optional for voice control
sounddevice>=0.4.6  # Alternative audio library
pyttsx3>=2.90  # Text-to-speech

# Speech Recognition
SpeechRecognition>=3.10.0

# Network Communication
cyclonedds>=0.10.0

# 2D Visualization (for SLAM) - REQUIRED
matplotlib>=3.7.0

# 3D Visualization (for SLAM) - OPTIONAL
# NOTE: open3d is OPTIONAL and only needed for advanced 3D point cloud visualization
# The app works fine without it using matplotlib for 2D SLAM visualization
# If you get 404 errors, skip this package - it's not critical
# open3d>=0.17.0  # Commented out - install only if needed

# Intel RealSense Camera (G1 has D435i) - OPTIONAL
# Needed for depth camera access on the G1 robot
# On Ubuntu: sudo apt install librealsense2-dkms librealsense2-utils
# Then: pip install pyrealsense2
# pyrealsense2>=2.50.0  # Commented out - install manually if needed

# Additional Utilities - OPTIONAL
# scipy is optional, only used for advanced features
# scipy>=1.11.0  # Commented out - install only if needed
# pyserial>=3.5  # Commented out - install only if needed
